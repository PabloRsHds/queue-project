export interface ResponseServiceManagementDto {
  serviceManagementId: string;
  name: string;
  code: string;
  description: string;
  departmentId: string;
  departmentName: string;
  active: boolean;
}
